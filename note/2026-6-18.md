# 2026-6-18
## 统一坐标系方向——让传感器的物理转动方向，与你程序里定义的角度正方向保持一致。
angleGyro = COMPUTE_ANGLE_GYRO((double)-mpu6050Data.GyroX, (double)angleSmooth);         // 滤波后，下次angleGyro的累加要在angleSmooth基础上累加    // 坑点，我这里方向弄错了，正转是负的，反转是正的，所以加个负号修正一下